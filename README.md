# shopease-app
A ShopEase App created using Blazor Web Assembly and Copilot.

Note: SQL injection is already prevented via parameterized queries. Razor output encoding also mitigates XSS by default, but the added sanitization helps if future inputs are added.
